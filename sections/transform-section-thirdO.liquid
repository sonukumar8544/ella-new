{% assign transform_section1 = product.metafields.custom.product_3_0_banner_details.value %}
{% if transform_section1 != blank %}
  {% for transform_value in transform_section1 %}
<section class="transformation-section" section{{ block.id }}>
  <div class="container">
    {% if transform_value.title1 != blank or transform_value.descriptions1 %}
    <h2 class="section-title">{{ transform_value.title1 }}</h2>
    <p class="section-subtitle">
    {{ transform_value.descriptions1 }}  
    </p>
    {% endif %}
    
    <div class="results-grid">
      {% if transform_value.count_title != blank or transform_value.description1 != blank %}
      <div class="result-box">
        <h3 class="percentage">{{ transform_value.count_title }}</h3>
        <p class="result-text">{{ transform_value.description1 }}</p>
      </div>
      {% endif %}
      {% if transform_value.count2 != blank or transform_value.description2 != blank %}
      <div class="result-box">
        <h3 class="percentage">{{ transform_value.count2 }}</h3>
        <p class="result-text">{{ transform_value.description2 }}</p>
      </div>
      {% endif %}
    {% if transform_value.count3 != blank or transform_value.description3 != blank %}
      <div class="result-box">
        <h3 class="percentage">{{ transform_value.count3 }}</h3>
        <p class="result-text">{{ transform_value.description3 }}</p>
      </div>
      {% endif %}
    {% if transform_value.count4 != blank or transform_value.description4 != blank %}
      <div class="result-box">
        <h3 class="percentage">{{ transform_value.count4 }}</h3>
        <p class="result-text">{{ transform_value.description4 }}</p>
      </div>
      {% endif %}
    {% if transform_value.count5 != blank or transform_value.description5 != blank %}
      <div class="result-box">
        <h3 class="percentage">{{ transform_value.count5 }}</h3>
        <p class="result-text">{{ transform_value.description5 }}</p>
      </div>
      {% endif %}
    </div>
  </div>
</section>
<style>
  .transformation-section {
  padding: 60px 20px;
  background-color: #fff;
  text-align: center;
}

.section-title {
  font-size: 32px;
  color: #00678c;
  margin-bottom: 15px;
  font-weight: 700;
}

.section-subtitle {
  font-size: 15px;
  color: #333;
  margin-bottom: 40px;
  line-height: 1.6;
}

.results-grid {
  display: flex;
  justify-content: space-between;
  gap: 20px;
  flex-wrap: wrap;
}

.result-box {
  flex: 1;
  min-width: 200px;
  margin: 10px 0;
}

.percentage {
  font-size: 32px;
  font-weight: 700;
  color: #333;
  margin-bottom: 10px;
}

.result-text {
  font-size: 13px;
  color: #444;
  max-width: 250px;
  margin: 0 auto;
  line-height: 1.4;
}

/* Responsive for tablets and mobile */
@media (max-width: 768px) {
  .results-grid {
    flex-direction: column;
    align-items: center;
  }

  .result-box {
    width: 100%;
    max-width: 300px;
  }
}

</style>
  {% endfor %}
  {% endif %}

{% schema %}
    {
  "name": "transform-section-thirdO",
  "settings": [
  ],
  "presets": [
    {
      "name": "transform-section-thirdO"
    }
  ]
}
{% endschema %}